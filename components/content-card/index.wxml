<view class="content-card">
  <view class="card-top" wx:if="{{cardtop}}">
    <view class="card-top-left">
      <view class="line"></view>
      <view class="content-name"><text class="pnameText">{{name}}</text></view>
    </view>
    <slot name="top-right" />
  </view>
  <view class="card-bottom l-content {{cardPadding?'card-padding':'padd'}}">
    <slot />
  </view>
</view>